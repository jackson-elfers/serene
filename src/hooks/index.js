import OwnerProfile from "./owner_profile.hook";

import axios from "axios";
import { routes, api } from "../config";

const method = {
  axios: axios,
  routes: routes,
  api: api,
};

export default {};
